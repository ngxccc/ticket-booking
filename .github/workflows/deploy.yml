name: CD

on:
  push:
    branches: ["main"]
    paths:
      - "src/**"
      - "!src/database/seeds/**"
      - "drizzle/**"
      - "package.json"
      - "bun.lock"
      - "tsconfig.json"
      - "nest-cli.json"
      - ".github/workflows/deploy.yml"
      - ".github/workflows/integration.yml"
      - ".github/setup-environment/**"
permissions:
  contents: read
  checks: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  integration:
    name: "Pre-Deploy Verification"
    permissions:
      contents: read
      checks: write
    uses: ./.github/workflows/integration.yml
    secrets: inherit
  deploy:
    name: Trigger Render Redeploy
    needs: integration
    runs-on: ubuntu-latest
    environment: production
    env:
      DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Environment
        uses: ./.github/setup-environment

      - name: Migrate Production Database
        run: bun run db:migrate prd

      - name: Call Render Deploy Hook
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
  #   env:
  #     TUNNEL_SERVICE_TOKEN_ID: ${{ secrets.CF_CLIENT_ID }}
  #     TUNNEL_SERVICE_TOKEN_SECRET: ${{ secrets.CF_CLIENT_SECRET }}
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v7
  #
  #     - name: Setup Bun
  #       uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: 1.3
  #
  #     - name: Build Application
  #       run: |
  #         bun install --frozen-lockfile
  #         bun run build
  #
  #     - name: Package Build Artifacts
  #       run: |
  #         tar -czf build.tar.gz dist bun.lock package.json docker scripts docker-compose.yml drizzle.config.ts
  #
  #     - name: Install Cloudflared
  #       run: |
  #         curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
  #         sudo dpkg -i cloudflared.deb
  #         rm cloudflared.deb
  #
  #     - name: Configure SSH and SSH Keys
  #       run: |
  #         mkdir -p ~/.ssh
  #         echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/ticket-booking-v2_key.pem
  #         chmod 600 ~/.ssh/ticket-booking-v2_key.pem
  #
  #         # Configure SSH config to use cloudflared proxy command
  #         cat <<EOF > ~/.ssh/config
  #         Host ticket-booking-vm
  #             HostName ssh-ticketbooking.ngxc.io.vn
  #             User azureuser
  #             ProxyCommand cloudflared access ssh --hostname %h
  #             IdentityFile ~/.ssh/ticket-booking-v2_key.pem
  #             StrictHostKeyChecking no
  #         EOF
  #
  #     - name: Upload Build Artifacts to VPS
  #       run: |
  #         scp build.tar.gz ticket-booking-vm:/home/azureuser/ticket-booking/build.tar.gz
  #
  #     - name: Deploy to VPS using SSH & Doppler Token
  #       run: |
  #         ssh ticket-booking-vm "export DOPPLER_TOKEN='${{ secrets.DOPPLER_TOKEN }}' DOPPLER_CONFIG='${{ secrets.DOPPLER_CONFIG || vars.DOPPLER_CONFIG || '' }}' && cd /home/azureuser/ticket-booking && tar -xzf build.tar.gz && ./scripts/redeploy.sh"
